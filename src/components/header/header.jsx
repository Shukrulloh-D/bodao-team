import styles from "./header.module.css";

export const Header = () => {
  return (
    <section className={styles.section}>
      <h2>Хедер</h2>
      {/* Здесь будет контент (наверное) */}
    </section>
  );
};
