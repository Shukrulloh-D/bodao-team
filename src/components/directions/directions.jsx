import styles from "./directions.module.css";

export const Directions = () => {
  return (
    <section className={styles.section}>
      <h2>Направления</h2>
      {/* Здесь будет контент */}
    </section>
  );
};
